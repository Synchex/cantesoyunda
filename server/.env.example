# ============================================
# Trivia Explain API - Environment Variables
# ============================================
# Copy this file to .env and fill in your values
# cp .env.example .env

# ============================================
# REQUIRED: OpenAI API Key
# ============================================
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-api-key-here

# ============================================
# OPTIONAL: Server Configuration
# ============================================

# Port to run the server on (default: 3001)
PORT=3001

# OpenAI model to use (default: gpt-4o-mini)
# Options: gpt-4o-mini (cheap, fast), gpt-4o (better quality), gpt-4-turbo
OPENAI_MODEL=gpt-4o-mini

# Frontend origin for CORS (default: http://localhost:3000)
ALLOWED_ORIGIN=http://localhost:3000

# Rate limit: max requests per hour per IP (default: 10)
RATE_LIMIT_MAX=10
